# kanban-flow

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build kanban-flow` to build the library.

## Running unit tests

Run `nx test kanban-flow` to execute the unit tests via [Vitest](https://vitest.dev/).
